{
  "_comment": "Provider-neutral base config. Provider/model + API key live in 00-provider.local.json (generated by onboard-agent.sh). gateway.host is NOT a valid key in OpenClaw schema — use --gateway-bind at install time. _comment keys stripped by merge-configs.sh.",
  "gateway": {
    "port": 18789
  },
  "agents": {
    "defaults": {
      "workspace": "~/.openclaw/workspace",
      "bootstrapMaxChars": 50000,
      "heartbeat": {
        "every": "10m"
      }
    }
  },
  "messages": {
    "groupChat": {
      "visibleReplies": "automatic"
    }
  }
}
