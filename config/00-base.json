{
  "_comment": "Provider-neutral base config. Provider/model + API key live in 00-provider.local.json (gitignored, generated by onboard-agent.sh).",
  "gateway": {
    "host": "127.0.0.1",
    "port": 18789
  },
  "agents": {
    "defaults": {
      "workspace": "~/.openclaw/workspace",
      "bootstrapMaxChars": 30000
    }
  }
}
