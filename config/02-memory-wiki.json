{
  "_comment": "vault.path is per-install (absolute path resolved at install time by onboard-agent.sh into 00-provider.local.json). OpenClaw's plugins.entries config does not expand ~ or ${HOME} — issue #21921.",
  "plugins": {
    "entries": {
      "memory-wiki": {
        "enabled": true,
        "config": {
          "vaultMode": "isolated",
          "vault": {
            "renderMode": "obsidian"
          },
          "obsidian": {
            "enabled": true,
            "useOfficialCli": false,
            "vaultName": "openclaw",
            "openAfterWrites": false
          }
        }
      }
    }
  }
}
