{
  "_comment": "Telegram is a CHANNEL, not a plugin. botToken accepts a SecretRef. allowFrom does NOT — it must be an array of literal user IDs (string|number), so it's resolved at install time by onboard-agent.sh into 00-provider.local.json.",
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": { "source": "env", "provider": "default", "id": "TELEGRAM_BOT_TOKEN" },
      "dmPolicy": "allowlist",
      "groupPolicy": "disabled"
    }
  }
}
